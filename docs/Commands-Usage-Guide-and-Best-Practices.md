# Everything Claude Code - Commands 详细使用手册与开发最佳实践

> 本文档整理自 Everything Claude Code 项目，涵盖所有 31 个命令的详细使用说明、开发流程最佳实践，以及多种开发模式的应用指南。

---

## 目录

1. [Commands 总览与分类](#1-commands-总览与分类)
2. [命令详细说明](#2-命令详细说明)
3. [开发流程最佳实践](#3-开发流程最佳实践)
4. [多种开发模式](#4-多种开发模式)
5. [快速参考表](#5-快速参考表)

---

## 1. Commands 总览与分类

### 1.1 命令分类概览

| 类别 | 命令数量 | 主要用途 |
|------|----------|----------|
| **规划与设计** | 4 | 功能规划、架构设计、任务分解 |
| **测试驱动开发** | 3 | TDD 流程、测试覆盖率 |
| **代码审查** | 4 | 代码质量、安全审计、语言特定审查 |
| **构建与修复** | 2 | 构建错误修复、重构清理 |
| **文档维护** | 2 | 文档更新、代码地图更新 |
| **持续学习** | 5 | 模式学习、技能创建、本能管理 |
| **多模型协作** | 5 | 多 Agent 协调、并行执行 |
| **会话管理** | 2 | 会话历史、检查点 |
| **服务管理** | 2 | PM2 服务、包管理器配置 |
| **验证与评估** | 2 | 综合验证、评估标准 |

### 1.2 命令速查表

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        COMMANDS 速查表                                   │
├─────────────────────────────────────────────────────────────────────────┤
│ 规划类: /plan, /orchestrate, /multi-plan, /multi-workflow              │
│ TDD类:  /tdd, /test-coverage, /e2e                                     │
│ 审查类: /code-review, /python-review, /go-review, /refactor-clean      │
│ 构建类: /build-fix, /go-build                                          │
│ 文档类: /update-docs, /update-codemaps                                 │
│ 学习类: /learn, /skill-create, /evolve, /instinct-*                    │
│ 协作类: /multi-execute, /multi-backend, /multi-frontend                │
│ 管理类: /sessions, /checkpoint, /verify, /eval, /pm2, /setup-pm        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 命令详细说明

### 2.1 规划与设计类命令

#### `/plan` - 实现计划

**用途**: 在编写代码前创建全面的实现计划

**何时使用**:
- 开始新功能开发
- 进行重大架构变更
- 复杂重构工作
- 多文件/组件受影响时
- 需求不明确时

**工作流程**:
1. 分析请求并重述需求
2. 分解为阶段性步骤
3. 识别组件间依赖
4. 评估风险和阻碍
5. 估算复杂度 (High/Medium/Low)
6. **等待用户确认后才执行**

**示例**:
```bash
/plan "Add user authentication with OAuth"

# 输出示例:
# Implementation Plan: User Authentication
# 
# Requirements Restatement:
# - 实现 OAuth 认证流程
# - 支持 Google/GitHub 登录
# 
# Phases:
# Phase 1: Database Schema (user, session tables)
# Phase 2: OAuth Provider Integration
# Phase 3: Frontend Components
# Phase 4: Testing & Security Review
#
# WAITING FOR CONFIRMATION: Proceed? (yes/no/modify)
```

**关键点**: 必须收到用户明确确认才会开始编码

---

#### `/orchestrate` - 多 Agent 编排

**用途**: 按顺序执行多个 Agent 完成复杂任务

**工作流类型**:

| 类型 | Agent 链 | 适用场景 |
|------|----------|----------|
| `feature` | planner → tdd-guide → code-reviewer → security-reviewer | 新功能开发 |
| `bugfix` | planner → tdd-guide → code-reviewer | Bug 修复 |
| `refactor` | architect → code-reviewer → tdd-guide | 安全重构 |
| `security` | security-reviewer → code-reviewer → architect | 安全审计 |

**示例**:
```bash
/orchestrate feature "Add payment processing"
/orchestrate bugfix "Fix authentication timeout"
/orchestrate refactor "Redesign caching layer"
/orchestrate custom "architect,tdd-guide,code-reviewer" "API redesign"
```

**交接文档格式**:
```markdown
## HANDOFF: planner -> tdd-guide

### Context
[已完成工作摘要]

### Findings
[关键发现或决策]

### Files Modified
[修改的文件列表]

### Open Questions
[未解决问题]

### Recommendations
[建议的下一步]
```

---

#### `/multi-plan` - 多模型协作规划

**用途**: 利用多个 AI 模型协作进行任务分解和规划

**协作模型**:
- **Codex**: 后端逻辑、算法、调试 (后端权威)
- **Gemini**: 前端 UI/UX、视觉设计 (前端专家)
- **Claude**: 编排、规划、执行、交付

**工作流程**:
1. 需求分析与澄清
2. 并行调用多模型进行方案设计
3. 综合各模型建议
4. 生成统一实施计划

---

#### `/multi-workflow` - 完整多模型开发流程

**用途**: 6 阶段结构化开发流程

**阶段**:
1. **Research** - 研究调研
2. **Ideation** - 创意构思
3. **Plan** - 详细规划
4. **Execute** - 执行实施
5. **Optimize** - 优化改进
6. **Review** - 审查验收

**智能路由**:
- 前端任务 → Gemini
- 后端任务 → Codex
- 协调整合 → Claude

---

### 2.2 测试驱动开发类命令

#### `/tdd` - 测试驱动开发

**用途**: 强制执行 TDD 方法论

**TDD 循环**:
```
RED → GREEN → REFACTOR → REPEAT

RED:      编写失败的测试
GREEN:    编写最小代码使测试通过
REFACTOR: 改进代码，保持测试通过
REPEAT:   下一个功能/场景
```

**执行步骤**:
1. **定义接口** - 先定义类型/接口
2. **编写失败测试** - 测试必须先失败 (RED)
3. **运行测试** - 验证失败原因正确
4. **最小实现** - 只写足够通过测试的代码 (GREEN)
5. **再次运行测试** - 验证通过
6. **重构** - 改进代码结构 (REFACTOR)
7. **检查覆盖率** - 确保 80%+ 覆盖率

**覆盖率要求**:
| 代码类型 | 最低覆盖率 |
|----------|------------|
| 一般代码 | 80% |
| 金融计算 | 100% |
| 认证逻辑 | 100% |
| 安全关键代码 | 100% |
| 核心业务逻辑 | 100% |

**最佳实践**:
```
✅ DO:
- 先写测试，再写实现
- 每次修改后运行测试
- 只写最小代码通过测试
- 测试通过后再重构
- 覆盖边缘情况和错误场景

❌ DON'T:
- 在测试前写实现
- 跳过测试运行
- 一次写太多代码
- 忽略失败的测试
- Mock 所有东西
```

---

#### `/test-coverage` - 测试覆盖率分析

**用途**: 分析测试覆盖率并生成缺失的测试

**工作流程**:
1. 运行带覆盖率的测试
2. 分析覆盖率报告
3. 识别低于 80% 阈值的文件
4. 为未覆盖的代码路径生成测试
5. 验证新测试通过
6. 显示前后覆盖率对比

**关注点**:
- Happy path 场景
- 错误处理
- 边缘情况 (null, undefined, empty)
- 边界条件

---

#### `/e2e` - 端到端测试

**用途**: 生成和运行 E2E 测试

**测试类型**:
- 关键用户流程
- 多步骤流程
- 全栈集成测试

---

### 2.3 代码审查类命令

#### `/code-review` - 通用代码审查

**用途**: 全面的代码质量审查

**审查内容**:
- 代码质量
- 安全性
- 可维护性
- 性能
- 最佳实践遵循

---

#### `/python-review` - Python 代码审查

**用途**: Python 特定的代码审查

**审查类别**:

| 级别 | 问题类型 |
|------|----------|
| **CRITICAL** | SQL/命令注入、不安全的 eval/exec、Pickle 反序列化、硬编码凭证 |
| **HIGH** | 缺少类型提示、可变默认参数、静默吞异常、未使用上下文管理器 |
| **MEDIUM** | PEP 8 违规、缺少文档字符串、print 代替 logging、魔法数字 |

**自动检查**:
```bash
mypy .                    # 类型检查
ruff check .              # Linting
black --check .           # 格式化
bandit -r .               # 安全扫描
pip-audit                 # 依赖审计
pytest --cov=app          # 测试覆盖率
```

**常见修复示例**:

```python
# 修复可变默认参数
# Before (Bad)
def process_items(items=[]):
    items.append("new")
    return items

# After (Good)
def process_items(items=None):
    if items is None:
        items = []
    items.append("new")
    return items

# 添加类型提示
# Before
def get_user(user_id):
    return db.find(user_id)

# After
def get_user(user_id: str) -> Optional[User]:
    return db.find(user_id)

# 使用上下文管理器
# Before
f = open("config.json")
data = f.read()
f.close()

# After
with open("config.json") as f:
    data = f.read()
```

---

#### `/go-review` - Go 代码审查

**用途**: Go 语言特定的代码审查

**审查内容**:
- Go 惯用法
- 错误处理
- 并发安全
- 性能优化

---

#### `/refactor-clean` - 重构清理

**用途**: 安全地识别和删除死代码

**工作流程**:
1. 运行死代码分析工具 (knip, depcheck, ts-prune)
2. 生成报告 `.reports/dead-code-analysis.md`
3. 按严重性分类:
   - **SAFE**: 测试文件、未使用工具
   - **CAUTION**: API 路由、组件
   - **DANGER**: 配置文件、主入口
4. 只提议安全删除
5. 每次删除前运行测试
6. 测试失败则回滚

**关键原则**: 删除代码前必须先运行测试！

---

### 2.4 构建与修复类命令

#### `/build-fix` - 修复构建错误

**用途**: 诊断和修复构建错误

**工作流程**:
1. 运行构建命令
2. 捕获错误信息
3. 分析错误原因
4. 提供修复建议
5. 应用修复
6. 验证构建成功

---

#### `/go-build` - Go 构建修复

**用途**: 修复 Go 项目构建错误

**处理内容**:
- 编译错误
- 依赖问题
- 模块配置

---

### 2.5 文档维护类命令

#### `/update-docs` - 更新文档

**用途**: 从源代码同步文档

**生成内容**:
1. 从 `package.json` 生成脚本参考表
2. 从 `.env.example` 提取环境变量文档
3. 生成 `docs/CONTRIB.md` (开发工作流)
4. 生成 `docs/RUNBOOK.md` (部署运维手册)
5. 识别过时文档 (90+ 天未修改)

---

#### `/update-codemaps` - 更新代码地图

**用途**: 分析代码库结构并更新架构文档

**生成文件**:
- `codemaps/architecture.md` - 整体架构
- `codemaps/backend.md` - 后端结构
- `codemaps/frontend.md` - 前端结构
- `codemaps/data.md` - 数据模型和 Schema

**特性**:
- 变更超过 30% 需用户确认
- 添加新鲜度时间戳
- 保存差异报告

---

### 2.6 持续学习类命令

#### `/learn` - 会话中学习

**用途**: 从当前会话中提取模式

**工作流程**:
1. 分析当前会话的交互
2. 识别重复模式
3. 提取可复用知识
4. 保存为技能或本能

---

#### `/skill-create` - 创建技能

**用途**: 从 Git 历史分析提取编码模式，生成 SKILL.md 文件

**使用方式**:
```bash
/skill-create                    # 分析当前仓库
/skill-create --commits 100      # 分析最近 100 次提交
/skill-create --output ./skills  # 自定义输出目录
/skill-create --instincts        # 同时生成 instincts
```

**分析步骤**:
1. **收集 Git 数据** - 提交历史、文件变更
2. **检测模式** - 提交约定、文件共变、工作流序列
3. **生成 SKILL.md** - 创建标准技能文件
4. **生成 Instincts** (可选) - 用于持续学习 v2

**检测的模式类型**:
| 模式 | 检测方法 |
|------|----------|
| 提交约定 | 正则匹配 (feat:, fix:, chore:) |
| 文件共变 | 总是一起变更的文件 |
| 工作流序列 | 重复的文件变更模式 |
| 架构 | 文件夹结构和命名约定 |
| 测试模式 | 测试文件位置、命名、覆盖率 |

---

#### `/evolve` - 演化技能

**用途**: 将相关的 instincts 聚类为技能

---

#### `/instinct-status` - 本能状态

**用途**: 查看已学习的本能及其置信度

```bash
/instinct-status        # 显示所有本能及置信度
```

---

#### `/instinct-import` - 导入本能

**用途**: 从他人导入本能

```bash
/instinct-import <file>  # 导入本能文件
```

---

#### `/instinct-export` - 导出本能

**用途**: 导出本能供分享

```bash
/instinct-export         # 导出所有本能
```

---

### 2.7 多模型协作类命令

#### `/multi-execute` - 多模型协作执行

**用途**: 多模型并行执行任务

**协作模型**:
- **Codex**: 后端任务
- **Gemini**: 前端任务
- **Claude**: 协调整合

---

#### `/multi-backend` - 后端多服务编排

**用途**: 后端专注的多模型工作流

**适用场景**:
- API 开发
- 数据库设计
- 后端服务集成

---

#### `/multi-frontend` - 前端多服务编排

**用途**: 前端专注的多模型工作流

**适用场景**:
- UI 组件开发
- 用户体验设计
- 前端性能优化

---

### 2.8 会话管理类命令

#### `/sessions` - 会话管理

**用途**: 管理 Claude Code 会话历史

**操作**:
```bash
/sessions                        # 列出所有会话
/sessions list --limit 10        # 显示 10 个会话
/sessions list --date 2026-02-01 # 按日期筛选
/sessions load <id|alias>        # 加载会话
/sessions alias <id> <name>      # 创建别名
/sessions info <id|alias>        # 显示会话详情
/sessions aliases                # 列出所有别名
```

---

#### `/checkpoint` - 保存检查点

**用途**: 保存验证状态检查点

**使用场景**:
- 完成重要里程碑后
- 开始风险操作前
- 切换任务前

---

### 2.9 服务管理类命令

#### `/pm2` - PM2 服务管理

**用途**: 自动分析项目并生成 PM2 服务命令

**服务检测**:
| 类型 | 检测方式 | 默认端口 |
|------|----------|----------|
| Vite | vite.config.* | 5173 |
| Next.js | next.config.* | 3000 |
| Nuxt | nuxt.config.* | 3000 |
| Express | server/backend 目录 | 3000 |
| FastAPI/Flask | requirements.txt | 8000 |
| Go | go.mod / main.go | 8080 |

**生成文件**:
```
project/
├── ecosystem.config.cjs           # PM2 配置
├── {backend}/start.cjs            # Python 包装器
└── .claude/
    ├── commands/
    │   ├── pm2-all.md             # 启动全部 + 监控
    │   ├── pm2-all-stop.md        # 停止全部
    │   ├── pm2-{port}.md          # 启动单个 + 日志
    │   └── pm2-status.md          # 查看状态
    └── scripts/
        └── pm2-monit.ps1          # PM2 监控面板
```

**常用命令**:
```bash
pm2 start ecosystem.config.cjs   # 首次启动
pm2 start all                    # 后续启动
pm2 stop all / pm2 restart all   # 停止/重启
pm2 logs / pm2 status / pm2 monit # 日志/状态/监控
pm2 save                         # 保存进程列表
pm2 resurrect                    # 恢复保存的列表
```

---

#### `/setup-pm` - 配置包管理器

**用途**: 配置首选包管理器 (npm/pnpm/yarn/bun)

**检测优先级**:
1. 环境变量 `CLAUDE_PACKAGE_MANAGER`
2. 项目配置 `.claude/package-manager.json`
3. package.json 的 `packageManager` 字段
4. Lock 文件检测
5. 全局配置 `~/.claude/package-manager.json`
6. 回退: 第一个可用的包管理器

**使用方式**:
```bash
node scripts/setup-package-manager.js --detect   # 检测当前
node scripts/setup-package-manager.js --global pnpm  # 设置全局
node scripts/setup-package-manager.js --project bun  # 设置项目
```

---

### 2.10 验证与评估类命令

#### `/verify` - 综合验证

**用途**: 对当前代码库状态进行全面验证

**验证顺序**:
1. **Build Check** - 构建检查
2. **Type Check** - 类型检查
3. **Lint Check** - Lint 检查
4. **Test Suite** - 测试套件
5. **Console.log Audit** - console.log 审计
6. **Git Status** - Git 状态

**输出格式**:
```
VERIFICATION: [PASS/FAIL]

Build:    [OK/FAIL]
Types:    [OK/X errors]
Lint:     [OK/X issues]
Tests:    [X/Y passed, Z% coverage]
Secrets:  [OK/X found]
Logs:     [OK/X console.logs]

Ready for PR: [YES/NO]
```

**参数**:
- `quick` - 只检查 build + types
- `full` - 全部检查 (默认)
- `pre-commit` - 提交前检查
- `pre-pr` - PR 前完整检查 + 安全扫描

---

#### `/eval` - 评估标准

**用途**: 根据标准评估代码/功能

---

## 3. 开发流程最佳实践

### 3.1 新系统开发流程

#### 🚀 完整新功能开发流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     新系统开发流程                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. /plan "功能描述"           ← 规划阶段                               │
│         ↓                                                               │
│  2. 用户确认计划               ← 确认阶段                               │
│         ↓                                                               │
│  3. /tdd                       ← TDD 实现阶段                           │
│         ↓                                                               │
│  4. /build-fix (如需要)        ← 构建修复                               │
│         ↓                                                               │
│  5. /code-review               ← 代码审查                               │
│         ↓                                                               │
│  6. /verify pre-pr             ← 验证阶段                               │
│         ↓                                                               │
│  7. 提交 PR                    ← 发布阶段                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 详细步骤说明

**Step 1: 规划 (`/plan`)**
```bash
/plan "Add user authentication with OAuth support"
```

输出:
- 需求重述
- 实施阶段分解
- 依赖识别
- 风险评估
- 复杂度估算

**Step 2: 确认计划**
- 审查生成的计划
- 提出修改意见: `modify: 增加 2FA 支持`
- 或确认: `yes` / `proceed`

**Step 3: TDD 实现 (`/tdd`)**
```bash
/tdd
```

执行 RED → GREEN → REFACTOR 循环:
1. 定义接口
2. 编写失败测试
3. 实现最小代码
4. 重构优化
5. 验证覆盖率 ≥ 80%

**Step 4: 构建修复 (如需要)**
```bash
/build-fix
```

**Step 5: 代码审查**
```bash
/code-review                # 通用审查
/python-review              # Python 项目
/go-review                  # Go 项目
```

**Step 6: 验证**
```bash
/verify pre-pr
```

检查:
- ✅ Build 通过
- ✅ 类型检查通过
- ✅ Lint 检查通过
- ✅ 测试通过 (80%+ 覆盖率)
- ✅ 无 console.log
- ✅ 无硬编码密钥

**Step 7: 提交 PR**

---

#### 🎯 快速功能开发 (简化流程)

适用于小型功能或紧急修复:

```bash
# 1. 快速规划
/plan "Add email validation"

# 2. 确认后直接 TDD
/tdd

# 3. 快速验证
/verify quick

# 4. 提交
```

---

#### 🏗️ 大型功能开发 (完整编排)

适用于复杂功能，需要多 Agent 协作:

```bash
# 使用完整编排流程
/orchestrate feature "Add real-time notification system"

# 或使用多模型协作
/multi-workflow "Build payment processing module"
```

**编排流程**:
```
planner → tdd-guide → code-reviewer → security-reviewer
```

**输出最终报告**:
```
ORCHESTRATION REPORT
====================
Workflow: feature
Task: Add real-time notification system

SUMMARY
-------
成功实现实时通知系统...

AGENT OUTPUTS
-------------
Planner: 完成 4 阶段规划
TDD Guide: 实现 12 个测试用例，覆盖率 92%
Code Reviewer: 发现 2 个建议改进
Security Reviewer: 无安全问题

RECOMMENDATION
--------------
✅ SHIP
```

---

### 3.2 旧系统维护/重构流程

#### 🔧 Bug 修复流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     Bug 修复流程                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 分析 Bug                   ← 理解问题                               │
│         ↓                                                               │
│  2. /tdd (编写复现测试)        ← 先写失败测试                           │
│         ↓                                                               │
│  3. 实现修复                   ← 最小修复                               │
│         ↓                                                               │
│  4. /code-review               ← 审查修复                               │
│         ↓                                                               │
│  5. /verify                    ← 验证无回归                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**关键原则**: 先写能复现 Bug 的测试，再修复

```bash
# 1. 使用编排进行 Bug 修复
/orchestrate bugfix "Fix authentication timeout issue"

# 或手动流程:
# 2. 先写复现测试
/tdd
# 在 TDD 中:
# - 编写能复现 bug 的测试 (应该失败)
# - 修复代码使测试通过
# - 添加边缘情况测试

# 3. 审查修复
/code-review

# 4. 验证
/verify
```

---

#### 🔄 安全重构流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     安全重构流程                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. /update-codemaps           ← 理解当前架构                           │
│         ↓                                                               │
│  2. /orchestrate refactor      ← 架构师 + TDD + 审查                    │
│         ↓                                                               │
│  3. /refactor-clean            ← 清理死代码                             │
│         ↓                                                               │
│  4. /test-coverage             ← 确保覆盖率                             │
│         ↓                                                               │
│  5. /verify full               ← 完整验证                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**详细步骤**:

```bash
# 1. 更新代码地图，理解当前架构
/update-codemaps

# 2. 使用重构编排
/orchestrate refactor "Redesign caching layer"
# 执行: architect → code-reviewer → tdd-guide

# 3. 清理死代码
/refactor-clean
# - 运行 knip, depcheck, ts-prune
# - 按安全级别分类
# - 每次删除前运行测试

# 4. 补充测试覆盖
/test-coverage
# - 识别低覆盖文件
# - 生成缺失测试
# - 确保 80%+ 覆盖率

# 5. 完整验证
/verify full
```

---

#### 🛡️ 遗留系统安全审计

```bash
# 使用安全编排
/orchestrate security "Audit authentication module"
# 执行: security-reviewer → code-reviewer → architect

# 或使用 AgentShield 工具
npx ecc-agentshield scan --opus --stream
```

---

### 3.3 日常开发工作流

#### 📅 每日开发流程

```bash
# 开始工作 - 加载昨天的会话
/sessions load yesterday

# 或创建新会话别名
/sessions alias 2026-02-19 today

# 查看项目状态
/verify quick

# 开始新功能
/plan "Today's feature"

# 实现
/tdd

# 结束时保存检查点
/checkpoint

# 学习今天的模式
/learn
```

#### 🔄 提交前检查清单

```bash
# 1. 验证代码
/verify pre-commit

# 2. 如有问题，修复
/build-fix

# 3. 代码审查
/code-review

# 4. 确认无问题后提交
git commit -m "feat: your feature"
```

#### 📝 PR 前检查清单

```bash
# 完整验证
/verify pre-pr

# 更新文档
/update-docs

# 安全扫描
npx ecc-agentshield scan
```

---

## 4. 多种开发模式

### 4.1 TDD 模式 (测试驱动开发)

#### 核心循环

```
┌──────────────────────────────────────────┐
│            TDD 循环                       │
│                                          │
│    ┌─────────┐                           │
│    │   RED   │ ← 写失败测试              │
│    └────┬────┘                           │
│         ↓                                │
│    ┌─────────┐                           │
│    │  GREEN  │ ← 最小实现                │
│    └────┬────┘                           │
│         ↓                                │
│    ┌─────────┐                           │
│    │REFACTOR │ ← 优化代码                │
│    └────┬────┘                           │
│         ↓                                │
│       REPEAT                             │
└──────────────────────────────────────────┘
```

#### 使用命令

```bash
# 启动 TDD 模式
/tdd

# TDD 完成后检查覆盖率
/test-coverage

# 生成 E2E 测试
/e2e
```

#### 测试类型金字塔

```
        ┌─────────┐
        │  E2E    │  ← /e2e (少量关键流程)
        ├─────────┤
        │集成测试  │  ← /tdd (API, 组件)
        ├─────────┤
        │单元测试  │  ← /tdd (函数级别)
        └─────────┘
```

#### 覆盖率标准

| 代码类型 | 最低覆盖率 | 命令 |
|----------|------------|------|
| 一般代码 | 80% | `/test-coverage` |
| 核心业务 | 100% | `/tdd` |
| 金融计算 | 100% | `/tdd` |
| 认证逻辑 | 100% | `/tdd` |

---

### 4.2 多模型协作模式

#### 模型分工

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    多模型协作架构                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│   │   Codex     │     │   Gemini    │     │   Claude    │              │
│   │  (后端权威)  │     │  (前端专家)  │     │  (协调者)   │              │
│   └──────┬──────┘     └──────┬──────┘     └──────┬──────┘              │
│          │                   │                   │                      │
│          ↓                   ↓                   ↓                      │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│   │  API 开发   │     │  UI 组件    │     │  任务分解   │              │
│   │  数据库设计  │     │  用户体验   │     │  流程编排   │              │
│   │  算法实现   │     │  视觉设计   │     │  结果整合   │              │
│   └─────────────┘     └─────────────┘     └─────────────┘              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 协作命令

```bash
# 多模型规划
/multi-plan "Build e-commerce checkout flow"

# 多模型执行
/multi-execute "Implement shopping cart API"

# 后端专注
/multi-backend "Design database schema for orders"

# 前端专注
/multi-frontend "Create checkout UI components"

# 完整工作流
/multi-workflow "Full-stack user dashboard"
```

#### 6 阶段工作流

```
Research → Ideation → Plan → Execute → Optimize → Review

1. Research:  调研需求和技术方案
2. Ideation:  多模型头脑风暴
3. Plan:      详细实施计划
4. Execute:   并行开发 (前后端分离)
5. Optimize:  性能和代码优化
6. Review:    综合审查和验收
```

---

### 4.3 安全审计模式

#### 安全审计流程

```bash
# 方式 1: 使用编排
/orchestrate security "Audit payment module"

# 方式 2: 使用 AgentShield
npx ecc-agentshield scan              # 快速扫描
npx ecc-agentshield scan --fix        # 自动修复安全问题
npx ecc-agentshield scan --opus       # 深度分析 (3 个 Opus Agent)
```

#### AgentShield 三 Agent 架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    AgentShield 红蓝对抗                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│   │  Attacker   │     │  Defender   │     │  Auditor    │              │
│   │  (红队)     │     │  (蓝队)     │     │  (审计员)   │              │
│   └──────┬──────┘     └──────┬──────┘     └──────┬──────┘              │
│          │                   │                   │                      │
│          ↓                   ↓                   ↓                      │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│   │ 寻找漏洞链  │     │ 评估防护   │     │ 综合评估    │              │
│   │ 攻击路径   │     │ 修复建议   │     │ 风险排序    │              │
│   └─────────────┘     └─────────────┘     └─────────────┘              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 扫描类别

| 类别 | 检查内容 |
|------|----------|
| Secrets Detection | 14 种密钥模式检测 |
| Permission Audit | 权限配置审计 |
| Hook Injection | Hook 注入分析 |
| MCP Server Risk | MCP 服务器风险评估 |
| Agent Config | Agent 配置审查 |

---

### 4.4 持续学习模式

#### 学习循环

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    持续学习循环                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   编码会话 → /learn → Instincts → /evolve → Skills → 应用到下次会话    │
│       ↑                                                    │            │
│       └────────────────────────────────────────────────────┘            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 学习命令

```bash
# 1. 从当前会话学习
/learn

# 2. 从 Git 历史创建技能
/skill-create
/skill-create --commits 200 --instincts

# 3. 查看学习到的本能
/instinct-status

# 4. 将本能演化为技能
/evolve

# 5. 导出/导入本能 (团队共享)
/instinct-export
/instinct-import team-patterns.yaml
```

#### 本能 vs 技能

| 特性 | Instinct (本能) | Skill (技能) |
|------|-----------------|--------------|
| 粒度 | 单一模式 | 完整工作流 |
| 置信度 | 0.0 - 1.0 | 稳定 |
| 来源 | 自动学习 | 手动或演化 |
| 格式 | YAML | Markdown |

---

### 4.5 文档驱动模式

#### 文档同步流程

```bash
# 1. 更新代码地图
/update-codemaps

# 2. 更新文档
/update-docs

# 3. 验证文档完整性
/verify
```

#### 生成的文档

| 文件 | 内容 |
|------|------|
| `codemaps/architecture.md` | 整体架构 |
| `codemaps/backend.md` | 后端结构 |
| `codemaps/frontend.md` | 前端结构 |
| `codemaps/data.md` | 数据模型 |
| `docs/CONTRIB.md` | 开发工作流 |
| `docs/RUNBOOK.md` | 运维手册 |

---

## 5. 快速参考表

### 5.1 命令速查

| 场景 | 命令 | 说明 |
|------|------|------|
| 开始新功能 | `/plan` | 创建实施计划 |
| TDD 开发 | `/tdd` | 测试驱动开发 |
| 代码审查 | `/code-review` | 通用审查 |
| Python 审查 | `/python-review` | Python 特定 |
| Go 审查 | `/go-review` | Go 特定 |
| 修复构建 | `/build-fix` | 构建错误修复 |
| 完整验证 | `/verify full` | 全面验证 |
| 提交前验证 | `/verify pre-commit` | 提交检查 |
| PR 前验证 | `/verify pre-pr` | PR 检查 |
| 清理死代码 | `/refactor-clean` | 安全清理 |
| 更新文档 | `/update-docs` | 文档同步 |
| 会话管理 | `/sessions` | 会话历史 |
| 学习模式 | `/learn` | 提取模式 |
| 创建技能 | `/skill-create` | 从 Git 学习 |
| 服务管理 | `/pm2` | PM2 配置 |

### 5.2 常见工作流

#### 新功能开发
```
/plan → 确认 → /tdd → /code-review → /verify pre-pr
```

#### Bug 修复
```
/orchestrate bugfix → /verify
```

#### 安全重构
```
/update-codemaps → /orchestrate refactor → /refactor-clean → /verify full
```

#### 安全审计
```
/orchestrate security 或 npx ecc-agentshield scan --opus
```

#### 多模型协作
```
/multi-plan → /multi-execute → /verify
```

### 5.3 Agent 选择指南

| 任务 | 推荐命令 | 调用的 Agent |
|------|----------|--------------|
| 规划新功能 | `/plan` | planner |
| 系统架构设计 | `/plan` + architect | architect |
| TDD 开发 | `/tdd` | tdd-guide |
| 代码审查 | `/code-review` | code-reviewer |
| 安全审计 | `/orchestrate security` | security-reviewer |
| 修复构建 | `/build-fix` | build-error-resolver |
| E2E 测试 | `/e2e` | e2e-runner |
| 清理代码 | `/refactor-clean` | refactor-cleaner |
| 更新文档 | `/update-docs` | doc-updater |
| Go 审查 | `/go-review` | go-reviewer |
| Python 审查 | `/python-review` | python-reviewer |
| 数据库审查 | (自动委派) | database-reviewer |

### 5.4 Token 优化设置

```json
// ~/.claude/settings.json
{
  "model": "sonnet",
  "env": {
    "MAX_THINKING_TOKENS": "10000",
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "50",
    "CLAUDE_CODE_SUBAGENT_MODEL": "haiku"
  }
}
```

| 命令 | 用途 |
|------|------|
| `/model sonnet` | 默认大多数任务 |
| `/model opus` | 复杂架构、深度推理 |
| `/clear` | 任务间重置 |
| `/compact` | 逻辑断点压缩 |
| `/cost` | 监控 Token 消耗 |

---

## 附录

### A. 相关资源

- [Everything Claude Code GitHub](https://github.com/affaan-m/everything-claude-code)
- [Shorthand Guide](https://x.com/affaanmustafa/status/2012378465664745795)
- [Longform Guide](https://x.com/affaanmustafa/status/2014040193557471352)
- [AgentShield](https://github.com/affaan-m/agentshield)
- [Skill Creator GitHub App](https://github.com/apps/skill-creator)

### B. 文件结构

```
~/.claude/
├── agents/           # Agent 定义
├── commands/         # 命令定义
├── skills/           # 技能定义
├── rules/            # 规则定义
│   ├── common/       # 通用规则
│   ├── typescript/   # TypeScript 规则
│   ├── python/       # Python 规则
│   └── golang/       # Go 规则
├── hooks/            # Hook 配置
├── sessions/         # 会话历史
└── settings.json     # 全局设置
```

### C. 版本历史

- **v1.4.1** - 修复 instinct import 内容丢失
- **v1.4.0** - 多语言规则、安装向导、PM2 支持
- **v1.3.0** - OpenCode 插件支持
- **v1.2.0** - Python/Django、Spring Boot 支持

---

*本文档基于 Everything Claude Code v1.4.1 整理*
*更新日期: 2026-02-19*

